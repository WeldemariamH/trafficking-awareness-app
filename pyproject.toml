[project]
name = "my-langchain-app"
version = "0.1.0"
dependencies = [
    "langchain",
    "langchain-google-genai",
    "langchain-community",
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

